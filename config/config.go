package config

// server config
var (
	PORT = ":3000"
)

// database config
var (
	DBHost     = "localhost"
	DBPort     = "5432"
	DBUser     = "user"
	DBPassword = "password"
	DBName     = "dbname"
)
